// Auto-generated by the g-digital MCP distribution pipeline.
// Source MCP: @g-digital/mcp-ead-enterprise-suite (1.4.2)
// Architecture: REST-direct (Epic 12 ADR 0008) — Bearer token fetched by the node.

import {
  ICredentialTestRequest,
  ICredentialType,
  INodeProperties,
} from 'n8n-workflow';

export class EadEnterpriseSuiteApi implements ICredentialType {
  name = 'eadEnterpriseSuiteApi';
  displayName = 'EAD Enterprise Suite API';
  documentationUrl = 'https://www.eadtrust.eu/soluciones-legaltech/enterprise-suite/';

  properties: INodeProperties[] = [
    {
      displayName: 'API Base URL',
      name: 'baseUrl',
      type: 'string',
      default: 'https://api-eadcustody.eadtrust.gocertius.io',
      description: 'Base URL of the EAD Enterprise Suite REST API. Leave as-is for the production environment (default: https://api-eadcustody.eadtrust.gocertius.io).',
    },
    {
      displayName: 'Auth Email',
      name: 'email',
      type: 'string',
      default: '',
      description: "Your EAD Enterprise Suite account email (Flow 1). Configure one of Flow 1 or Flow 2.",
    },
    {
      displayName: 'Auth Password',
      name: 'password',
      type: 'string',
      typeOptions: { password: true },
      default: '',
      description: "Your EAD Enterprise Suite account password (Flow 1, email/password accounts) (See https://www.eadtrust.eu/soluciones-legaltech/enterprise-suite/ for credential acquisition.)",
    },
  ];

  test: ICredentialTestRequest = {
    request: {
      baseURL: '={{$credentials.baseUrl}}',
      url: '/session',
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: {
        email: '={{$credentials.email}}',
        password: '={{$credentials.password}}',
      },
    },
  };
}
