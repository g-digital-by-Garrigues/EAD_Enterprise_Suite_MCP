// Auto-generated by @suite/generator. Do not edit.
// Sourced from operation: ListDocumentSignatoriesController_run (GET /case-files/{caseFileId}/signature-requests/{requestId}/documents/{documentId}/signatories)

import { defineTool } from "../core/index.js";
import { z } from "zod";
import { createClient, createConfig } from "../api/client/index.js";
import { listDocumentSignatoriesControllerRun } from "../api/sdk.gen.js";
import { zListDocumentSignatoriesControllerRunPath } from "../api/zod.gen.js";
import { zListDocumentSignatoriesControllerRunQuery } from "../api/zod.gen.js";
const inputSchema = z.object({
  ...zListDocumentSignatoriesControllerRunPath.shape,
  ...zListDocumentSignatoriesControllerRunQuery.shape,
});

export const signature_document_list = defineTool({
  name: "signature_document_list",
  description: "Performs the signature_document_list operation against the GoCertius API. Review the API documentation for full field details.",
  inputSchema,
  annotations: {
    destructive: false,
    idempotent: true,
    requiresUserConfirmation: false,
  },
  pollable: false,
  idempotencyWindowSeconds: 60,
  async execute(input, ctx) {
    const token = ctx.auth?.token ?? "";
    const sdkClient = createClient(
      createConfig({
        baseUrl: process.env.MCP_API_BASE_URL ?? "",
        headers: {
          Authorization: `Bearer ${token}`,
          ...(ctx.correlationId ? { "X-Correlation-Id": ctx.correlationId } : {}),
        },
      }),
    );

    // biome-ignore lint/suspicious/noExplicitAny: generated SDK call — input shape validated above
    const inp = input as any;
    // biome-ignore lint/suspicious/noExplicitAny: generated SDK function — types validated at generation time
    const sdkFn = listDocumentSignatoriesControllerRun as (opts: any) => Promise<any>;
    const response = await sdkFn({
      client: sdkClient,
      path: zListDocumentSignatoriesControllerRunPath.parse(inp),
      query: zListDocumentSignatoriesControllerRunQuery.parse(inp),
    });

    if (response.error !== undefined) {
      const msg = typeof response.error === "object" && response.error !== null && "message" in response.error
        ? String(response.error.message)
        : JSON.stringify(response.error);
      throw new Error(msg);
    }
    return response.data;
  },
});
